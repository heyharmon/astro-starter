---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import StatBand from "../components/StatBand.astro";
import FounderNote from "../components/FounderNote.astro";
import SectionHeading from "../components/SectionHeading.astro";
import Placeholder from "../components/Placeholder.astro";
import Button from "../components/Button.astro";
import ProgramCard from "../components/ProgramCard.astro";
import FeatureGrid from "../components/FeatureGrid.astro";
import Schedule from "../components/Schedule.astro";
import Testimonials from "../components/Testimonials.astro";
import BannerCta from "../components/BannerCta.astro";
import CtaBand from "../components/CtaBand.astro";
import siteMeta from "../data/site-meta.json";
import siteContent from "../data/site-content.json";

const {
  hero,
  stats,
  founder,
  approach,
  programsPreview,
  programs,
  features,
  schedule,
  testimonials,
  enrollmentCta,
  cta,
} = siteContent;
---

<BaseLayout title="Home" description={siteMeta.description}>
  <Hero {...hero} />

  <!-- Programs preview — surfaced right after the hero -->
  <section class="section section--tight programs-preview">
    <div class="wrap">
      <SectionHeading
        heading={programsPreview.heading}
        actionHref={programsPreview.cta.href}
        actionLabel={programsPreview.cta.label}
      />
      <div class="prog-grid" data-stagger>
        {programs.map((p) => <ProgramCard program={p} />)}
      </div>
    </div>
  </section>

  <StatBand stats={stats} />

  <FounderNote {...founder} />

  <!-- Approach — copy leads (flips the founder section's art-left rhythm) -->
  <section class="section approach">
    <div class="wrap approach-grid">
      <div class="approach-copy">
        <h2 class="approach-h">{approach.heading}</h2>
        <p class="lead">{approach.lead}</p>
        <p class="approach-body">{approach.body}</p>
        <Button href={approach.cta.href} variant="ghost" arrow class="approach-cta">
          {approach.cta.label}
        </Button>
      </div>
      <div class="approach-art">
        <Placeholder label={approach.images[0].label} ratio={approach.images[0].ratio} />
        <Placeholder label={approach.images[1].label} ratio={approach.images[1].ratio} />
      </div>
    </div>
  </section>

  <FeatureGrid eyebrow={features.eyebrow} heading={features.heading} items={features.items} />

  <Schedule
    eyebrow={schedule.eyebrow}
    heading={schedule.heading}
    lead={schedule.lead}
    image={schedule.image}
    items={schedule.items}
  />

  <Testimonials
    eyebrow={testimonials.eyebrow}
    heading={testimonials.heading}
    rating={testimonials.rating}
    items={testimonials.items}
  />

  <BannerCta {...enrollmentCta} />

  <CtaBand heading={cta.heading} body={cta.body} button={cta.button} />
</BaseLayout>

<style>
  .approach-grid {
    display: grid;
    grid-template-columns: 1fr 1.05fr;
    gap: clamp(2rem, 5vw, 4.5rem);
    align-items: center;
  }
  .approach-h {
    font-size: var(--step-3);
    margin: 0 0 1.3rem;
    max-width: 14ch;
  }
  .approach-body {
    margin-top: 1.1rem;
    color: var(--color-ink-soft);
  }
  .approach-cta {
    margin-top: 1.8rem;
  }
  .approach-art {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }
  .approach-art :global(.ph):last-child {
    margin-top: 2.4rem;
  }
  @media (max-width: 820px) {
    .approach-grid {
      grid-template-columns: 1fr;
    }
    .approach-art { order: -1; }
    .approach-h { max-width: 18ch; }
  }

  .prog-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: clamp(1.5rem, 3vw, 2.6rem) clamp(1.5rem, 4vw, 3rem);
    margin-top: 2.6rem;
  }
  @media (max-width: 560px) {
    .prog-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
